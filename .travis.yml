language: python
python:
  - "3.6"

before_install:
  - wget https://web.gin.g-node.org/G-Node/gin-cli-releases/raw/master/gin-cli-latest.deb
  - sudo dpkg -i gin-cli-latest.deb
  - gin get heilerich/NeuroneTestdata
  - cd NeuroneTestdata
  - gin get-content .
  - cd ..
  - mv NeuroneTestdata test/test_data

install:
  - pip install -r test/test_requirements.txt
  - pip install .
  - pip install python-coveralls
  - pip install coverage

script:
  - coverage run -m unittest discover -s test -t .

after_success:
  - coveralls